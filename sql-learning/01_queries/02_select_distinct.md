# 03. Streaming Wars

> **Source:** [Codédex](https://www.codedex.io/) | SQL Chapter

## 🎯 Core Concept
The `DISTINCT` keyword removes duplicate values from your query results, returning only unique values from a specified column.

## 📝 Key Syntax

### Basic SELECT (with duplicates)
```sql
-- Returns all values, including duplicates
SELECT column_name
FROM table_name;